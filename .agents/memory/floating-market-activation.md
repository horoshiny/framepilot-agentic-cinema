---
name: Floating-market authorization
description: Eligibility evidence and controlled authorization rollover rules for the floating-ocean-market First Cut path.
---

The browser-owned controlled-test claim is generic: it uses the current validated multimodal scene context and must show an actionable disabled reason when evidence or allowance prerequisites are incomplete.

**Why:** A prior guard looked for entity arrays directly on the shot plan, so valid multimodal scenes were treated as ineligible. Automatic activation also bound state before the user’s explicit click. The durable ledger retained a singleton index that blocked a new pending authorization after an earlier controlled attempt had been consumed.

**How to apply:** Keep activation button-only, session-bound, and provider-free. Validate `vertex_multimodal`, storyboard ownership, current scene identity, First Cut kind/model, and absence of an active First Cut on the server. Revoke only an unused stale authorization with `abandoned_session`; treat consumed and revoked audit rows as history, not pending blockers.